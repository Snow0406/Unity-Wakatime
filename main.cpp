#include <iostream>
#include <windows.h>

int main() {
    std::cout << "Unity WakaTime Monitor Starting..." << std::endl;

    // 일단 기본 테스트
    std::cout << "Press Enter to exit..." << std::endl;
    std::cin.get();

    return 0;
}